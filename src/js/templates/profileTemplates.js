// export function profileTemplate(profileData) {
//    const profileContainer = helpers.createDiv(['container', 'd-flex', 'flex-column', 'justify-content-center', 'align-items-center']);
//     const profileTitle = helpers.createH1(['fw-bold', 'text-capitalize', 'mt-2'], profileData.title);
//     const profileDescription = helpers.createP(['text-center', 'mb-5'], profileData.description);

//     profileContainer.append(profileTitle, profileDescription);
//     return profileContainer;

// }
